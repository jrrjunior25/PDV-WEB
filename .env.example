# Exemplo de variáveis de ambiente (NUNCA comitar .env com chaves reais)

# Chave do Gemini/GenAI — mantenha esta chave apenas no servidor/backend
GEMINI_API_KEY=your_gemini_api_key_here

# Porta do backend local
PORT=4000
